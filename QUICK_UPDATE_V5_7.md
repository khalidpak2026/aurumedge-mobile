# Mobile v5.7 quick update

1. Back up the BRAIN state.
2. Replace root files from the ZIP.
3. Inside GitHub `gold_web_terminal/`, replace every `.py` file from the ZIP's matching folder.
4. Replace `.github/workflows/aurumedge_signal_watch.yml`.
5. Do not overwrite `data/adaptive_state.json`.
6. Keep existing Streamlit and GitHub secrets.
7. Commit: `Add market structure AVWAP and volume profile v5.7`.
8. Verify header: `5.7.0-mobile-avwap-volume-profile`.
